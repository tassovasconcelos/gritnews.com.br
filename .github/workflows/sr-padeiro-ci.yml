name: Sr. Padeiro CI

on:
  push:
    branches: [main]
    paths:
      - 'apps/sr-padeiro/**'
      - '.github/workflows/sr-padeiro-ci.yml'
  pull_request:
    paths:
      - 'apps/sr-padeiro/**'
      - '.github/workflows/sr-padeiro-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sr-padeiro-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/sr-padeiro
    env:
      VITE_SUPABASE_URL: https://pcrwtoddavpvkaxwtstc.supabase.co
      VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SRP_SUPABASE_PUBLISHABLE_KEY }}
      VITE_APP_URL: https://srpadeiro.gritnews.com.br
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm run build
      - name: Smoke test
        run: pnpm run smoke
