import { useState } from 'react';
import { getMarketingConsent,setMarketingConsent } from './lib/tracking';
export default function MarketingConsent(){const [visible,setVisible]=useState(()=>getMarketingConsent()===null);if(!visible)return null;function choose(value:'accepted'|'declined'){setMarketingConsent(value);setVisible(false)}return <aside className="marketing-consent" role="dialog" aria-label="Preferências de privacidade"><div><strong>Sua privacidade importa</strong><p>Usamos métricas de Google e Meta para entender campanhas e melhorar o Sr. Padeiro. Você pode recusar sem perder nenhuma função.</p></div><div className="marketing-consent-actions"><button onClick={()=>choose('declined')}>Só essenciais</button><button className="btn-orange" onClick={()=>choose('accepted')}>Aceitar métricas</button></div></aside>}
