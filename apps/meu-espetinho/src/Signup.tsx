import { FormEvent, useState } from 'react';
import { ArrowLeft, Check, Flame } from 'lucide-react';
import { supabase, supabaseConfigured } from './lib/supabase';
import './auth.css';
export default function Signup(){const[mode,setMode]=useState<'signup'|'login'>('signup');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!supabase||!supabaseConfigured){setMsg('Conexão com a nuvem indisponível.');return}setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const email=String(f.get('email')||'');const password=String(f.get('password')||'');if(mode==='login'){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href='/app';setBusy(false);return}const full_name=String(f.get('name')||'');const business_name=String(f.get('business')||'');const phone=String(f.get('phone')||'');const{data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name,business_name,phone},emailRedirectTo:'https://app.meuespetinho.gritnews.com.br/app'}});if(error)setMsg(error.message);else if(data.session)location.href='/app';else setMsg('Cadastro criado. Confirme o e-mail para entrar e iniciar seu teste.');setBusy(false)}return <div className="auth-page"><a className="auth-back" href="/"><ArrowLeft/> Voltar</a><div className="auth-brand"><span><Flame/></span><div><b>MEU</b><strong>ESPETINHO</strong><small>SEU NEGÓCIO NO CONTROLE</small></div></div><div className="auth-grid"><section><span className="auth-kicker">3 DIAS GRÁTIS</span><h1>Comece hoje a organizar seu espetinho.</h1><p>Crie sua conta, cadastre seu negócio e use o sistema antes de decidir pela assinatura.</p><ul><li><Check/>Comandas e vendas</li><li><Check/>Produtos e clientes</li><li><Check/>Dashboard gerencial</li><li><Check/>R$ 89/mês após o período gratuito</li></ul></section><form onSubmit={submit}><h2>{mode==='signup'?'Criar minha conta':'Entrar no Meu Espetinho'}</h2>{mode==='signup'&&<><label>Seu nome<input name="name" required placeholder="Como podemos chamar você?"/></label><label>Nome do negócio<input name="business" required placeholder="Ex.: Espetinho do João"/></label><label>WhatsApp<input name="phone" required placeholder="(85) 99999-9999"/></label></>}<label>E-mail<input name="email" type="email" required/></label><label>Senha<input name="password" type="password" minLength={6} required/></label><button disabled={busy}>{busy?'Aguarde...':mode==='signup'?'Criar conta e testar 3 dias':'Entrar'}</button>{msg&&<p className="auth-message">{msg}</p>}<small>{mode==='signup'?'Já possui uma conta?':'Ainda não possui conta?'} <a onClick={(e)=>{e.preventDefault();setMode(mode==='signup'?'login':'signup')}} href="#">{mode==='signup'?'Entrar':'Começar grátis'}</a></small></form></div></div>