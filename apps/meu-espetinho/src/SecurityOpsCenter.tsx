import { useEffect, useMemo, useState } from 'react';
import { Activity, CheckCircle2, RefreshCw, ShieldCheck, UserCog, XCircle } from 'lucide-react';
import { supabase } from './lib/supabase';
import { GRIT_PRODUCTS } from './gritCommercial';
import './admin.css';
import './admin-workspace.css';

type AdminUser={user_id:string;role?:string;active?:boolean};
type Health={ok:boolean;release?:string;control_center?:string;crm?:string};

export default function SecurityOpsCenter(){
 const[session,setSession]=useState<any>(null);const[allowed,setAllowed]=useState<boolean|null>(null);const[admins,setAdmins]=useState<AdminUser[]>([]);const[health,setHealth]=useState<Health|null>(null);const[busy,setBusy]=useState(false);
 async function refresh(){const client=supabase;if(!client)return;setBusy(true);try{const{data:a}=await client.from('admin_users').select('user_id,role,active').limit(200);setAdmins((a||[]) as AdminUser[]);try{const r=await fetch('/deploy-health.json',{cache:'no-store'});setHealth(r.ok?await r.json():{ok:false})}catch{setHealth({ok:false})}}finally{setBusy(false)}}
 useEffect(()=>{const client=supabase;if(!client){setAllowed(false);return}client.auth.getSession().then(async({data})=>{setSession(data.session);if(!data.session){setAllowed(false);return}const{data:a}=await client.from('admin_users').select('role,active').eq('user_id',data.session.user.id).maybeSingle();const ok=Boolean(a?.active&&['superadmin','owner','admin'].includes(String(a.role||'').toLowerCase()));setAllowed(ok);if(ok)await refresh()})},[]);
 const active=useMemo(()=>admins.filter(a=>a.active),[admins]);const superadmins=useMemo(()=>active.filter(a=>String(a.role||'').toLowerCase()==='superadmin'),[active]);
 if(!session)return <div className="admin-login"><div className="admin-login-card"><ShieldCheck size={36}/><h1>Security & Operations Center</h1><p>Entre pelo gerenciador administrativo para continuar.</p><a href="/admin">Abrir gerenciador</a></div></div>;
 if(allowed===false)return <div className="admin-login"><div className="admin-login-card"><XCircle size={36}/><h1>Acesso protegido</h1><p>Seu perfil não possui permissão administrativa para esta área.</p></div></div>;
 if(allowed===null)return <div className="admin-loading">Validando segurança...</div>;
 return <div className="admin-shell admin-workspace"><main style={{width:'100%'}}><header className="admin-main-header"><div><small>GRIT SAAS FACTORY</small><h1>Security & Operations Center</h1><p>Governança, acessos administrativos, publicação e postura operacional dos produtos.</p></div><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><a href="/admin/apps">Central GRIT</a><button onClick={refresh}><RefreshCw className={busy?'spin':''}/> Atualizar</button></div></header><div className="admin-screen-stack"><section className="admin-metrics"><article><span>Admins ativos</span><strong>{active.length}</strong><small>{admins.length} cadastro(s) encontrados</small></article><article><span>Super Admins</span><strong>{superadmins.length}</strong><small>privilégio máximo</small></article><article><span>Release publicado</span><strong>{health?.ok?'OK':'Verificar'}</strong><small>{health?.release||'sem marcador'}</small></article><article><span>Produtos mapeados</span><strong>{GRIT_PRODUCTS.length}</strong><small>catálogo central</small></article></section><section className="admin-panel"><div className="panel-title"><div><small>ACESSOS ADMINISTRATIVOS</small><h2>Privilégios ativos</h2><p>Somente contas administrativas ativas devem acessar os centros transversais da GRIT.</p></div></div><div style={{overflowX:'auto'}}><table className="admin-table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th></tr></thead><tbody>{admins.map(a=><tr key={a.user_id}><td><UserCog size={14}/> {a.user_id}</td><td>{a.role||'—'}</td><td>{a.active?<span><CheckCircle2 size={14}/> Ativo</span>:<span><XCircle size={14}/> Inativo</span>}</td></tr>)}{!admins.length&&<tr><td colSpan={3}>Nenhum registro administrativo visível para este perfil.</td></tr>}</tbody></table></div></section><section className="admin-panel"><div className="panel-title"><div><small>POSTURA DE SEGURANÇA</small><h2>Gate operacional</h2></div></div><div className="admin-grid">{[['Identidade','Supabase Auth; sem senha de negócio no frontend.'],['Segregação','RLS por tenant/organização deve permanecer obrigatório.'],['Privilégio','Admin transversal somente via admin_users ativo.'],['Publicação','Deploy precisa de CI verde e marcador de release.'],['Auditoria','Mudanças comerciais sensíveis devem gerar histórico.'],['Segredos','service_role e credenciais privadas nunca no frontend.']].map(([t,b])=><article className="admin-panel" key={t}><ShieldCheck size={20}/><h3>{t}</h3><p>{b}</p></article>)}</div></section><section className="admin-panel"><div className="panel-title"><div><small>SAÚDE DE PUBLICAÇÃO</small><h2>Estado conhecido do ambiente</h2></div></div><p><Activity size={15}/> Control Center: <strong>{health?.control_center||'não confirmado'}</strong></p><p>CRM multi-app: <strong>{health?.crm||'não confirmado'}</strong></p><p>Marcador: <strong>{health?.release||'não confirmado'}</strong></p><small>Este painel não substitui monitoramento externo; ele confirma apenas os sinais publicados pelo próprio ambiente.</small></section></div></main></div>;
}
